## 1. Implementation
- [ ] 1.1 Add `dueDate` support to todo creation in the client form, app state flow, API client, server route, and persistence layer
- [ ] 1.2 Render due dates in todo items and mark overdue non-done items with distinct styling
- [ ] 1.3 Add inline title editing in `TodoItem` with save-on-enter-or-blur and cancel-on-escape behavior
- [ ] 1.4 Add a stats filtering contract for `GET /api/todos/stats?from=...&to=...` in the server service layer
- [ ] 1.5 Add clearly labeled mock integration points for missing teammate-owned `FilterBar` and `StatsPage` files
- [ ] 1.6 Manually verify create, edit, toggle, and overdue display flows
